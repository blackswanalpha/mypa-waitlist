# ─────────────────────────────────────────────────────────────────────────────
# mypa-waitlist environment variables
# Copy to .env.local for local dev. NEVER commit real secrets.
# ─────────────────────────────────────────────────────────────────────────────

# Next.js (public) — written automatically by `npx convex dev`
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud
CONVEX_DEPLOYMENT=dev:your-deployment

# Public site origin — used for metadataBase (Open Graph / canonical URLs).
# Set to the real marketing-site origin in prod.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ─────────────────────────────────────────────────────────────────────────────
# The following live on the CONVEX deployment, NOT in this file.
# Set them with:  npx convex env set <NAME> "<value>"
#
#   ADMIN_EMAILS        comma-separated allowlist of admin emails
#                       e.g.  npx convex env set ADMIN_EMAILS "you@example.com"
#   ALLOW_ADMIN_SIGNUP  admin ACCOUNT CREATION gate — unset by default. Set to
#                       "true" only while bootstrapping an admin account, then
#                       unset it again (npx convex env remove ALLOW_ADMIN_SIGNUP).
#                       Sign-in works regardless.
#   RESEND_API_KEY      Resend API key (re_...)
#   RESEND_TEST_MODE    defaults to TEST MODE ON — emails only reach Resend's
#                       test addresses. Set to "false" in prod to deliver for real.
#   RESEND_FROM         From header, e.g. 'MyPA <hello@yourdomain.com>'
#                       (defaults to Resend's onboarding sender)
#   SITE_URL            the app origin (http://localhost:3000 in dev; Vercel URL in prod)
#   JWT_PRIVATE_KEY     generated by `npx @convex-dev/auth`
#   JWKS                generated by `npx @convex-dev/auth`
#
# Whitelist → backend sync (the admin dashboard's "Sync to backend" button):
#
#   BACKEND_URL          product-backend origin, e.g. https://backend.mypa.computer
#   BACKEND_SERVICE_KEY  shared secret sent as X-Service-Key — must equal the
#                        backend's CONVEX_SERVICE_KEY
# ─────────────────────────────────────────────────────────────────────────────

# ─────────────────────────────────────────────────────────────────────────────
# Docker (self-hosted Convex) — see README §8. `scripts/docker-init.sh` writes
# these to .env.local automatically; they replace CONVEX_DEPLOYMENT:
#
#   CONVEX_SELF_HOSTED_URL=http://127.0.0.1:3210
#   CONVEX_SELF_HOSTED_ADMIN_KEY='convex-self-hosted|...'
#
# docker-compose.yml knobs (all optional): CONVEX_TAG (image pin), CONVEX_PORT,
# CONVEX_SITE_PORT, CONVEX_DASHBOARD_PORT, WEB_PORT.
# ─────────────────────────────────────────────────────────────────────────────
